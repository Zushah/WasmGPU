interface:
  display_name: "Prepare Release"
  short_description: "Prepare WasmGPU releases"
  default_prompt: "Use $prepare-release in audit mode to audit the release delta and stop at the approval checkpoint."
